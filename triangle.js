const readlineSync = require("readline-Sync");

const base = Number(question.readlineSync("Enter base: "));
const height = Number(question.readlineSync("Enter height: "))
const hypotenuse = Number(question.readlineSync("Enter hypotenuse: "))

let a = base
let b = height
let c = hypotenuse

if (c ** 2 = a ** 2 + b ** 2 ) {
  console.log("Yes, that's a right triangle!");
}else {
  console.log("Nope...not a right triangle.");
}
